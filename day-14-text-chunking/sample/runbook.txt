Rollback runbook for checkout-api. First confirm the active deployment version and the last stable version. Pause new deployments while the incident is active. Deploy the previous stable artifact. Watch error rate, latency, saturation, and database connection pool metrics. If rollback does not reduce errors within ten minutes, escalate to the platform on-call and database on-call. After recovery, write an incident summary and add follow-up actions for retry behavior and load testing.
